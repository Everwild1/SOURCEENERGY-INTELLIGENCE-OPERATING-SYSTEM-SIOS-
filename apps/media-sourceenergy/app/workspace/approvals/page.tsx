import { getApprovalQueue } from '@/lib/media/workspace';
export default async function ApprovalsPage(){const items=await getApprovalQueue();return <div className="shell page-stack"><section><p className="eyebrow">Institutional approval</p><h1>Approval queue</h1><p>Readiness indicators are advisory. Approval and publication remain separate governed commands.</p></section><section className="route-grid">{items.map(i=><article className="route-card" key={i.content_id}><p className="eyebrow">{i.organization_oid} · {i.lifecycle_status}</p><h2>{i.title}</h2><p>Version {i.current_version} · unresolved claims {i.unresolved_claims} · pending reviews {i.pending_reviews}</p></article>)}</section></div>}
