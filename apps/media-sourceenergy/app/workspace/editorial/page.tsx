import { getEditorialQueue } from '@/lib/media/workspace';
export default async function EditorialPage(){const items=await getEditorialQueue();return <div className="shell page-stack"><section><p className="eyebrow">Workspace</p><h1>Editorial queue</h1><p>Organization-scoped content visible to the authenticated principal. Mutation authority remains command-controlled.</p></section><section className="route-grid">{items.map(i=><article className="route-card" key={i.content_id}><p className="eyebrow">{i.organization_oid} · {i.lifecycle_status}</p><h2>{i.title}</h2><p>{i.content_type} · {i.representation_class} · v{i.current_version}</p></article>)}</section></div>}
