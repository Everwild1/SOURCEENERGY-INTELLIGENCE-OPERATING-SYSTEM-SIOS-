import { getEvidenceQueue } from '@/lib/media/workspace';
export default async function EvidencePage(){const items=await getEvidenceQueue();return <div className="shell page-stack"><section><p className="eyebrow">Fact validation</p><h1>Evidence & claims</h1><p>Unresolved claims requiring authorized validation. Queue access does not itself validate a claim.</p></section><section className="route-grid">{items.map(i=><article className="route-card" key={i.claim_id}><p className="eyebrow">{i.materiality} · {i.verification_state}</p><h2>{i.title}</h2><p>{i.claim_text}</p>{i.authoritative_source_required&&<p><strong>Authoritative source required</strong></p>}</article>)}</section></div>}
